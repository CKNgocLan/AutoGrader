package grader;

import java.util.Scanner;

public class StudentSubmission {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);
        int a = sc.nextInt();
        int b = sc.nextInt();
        // Example: students might compute sum, average, max, product, etc.
        System.out.println(a + b);   // Change this to match lab requirement
    }
}